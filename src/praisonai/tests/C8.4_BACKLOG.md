# C8.4 Backlog — main.py Thinning Epic

> Structural follow-up to C8 reverse-import elimination. Decompose 7,492-line `praisonai_code/cli/main.py` into domain modules.

## Status

| Phase | Description | Status |
|-------|-------------|--------|
| 0 | Spec, dead-branch cleanup, line-count gate | Done |
| 1 | Wire stubs + env_security.py | Done |
| 2 | subcommand_handlers.py (no workflow) | Done |
| 2b | argparse_builder.py | Done |
| 3 | workflow_commands.py | Done |
| 5 | direct_prompt.py | Done |
| 4 | legacy_dispatch.py | Done |
| 6+7 | praison_ai.py + thin main.py ≤300 | Done |
| 8 | interactive_legacy.py | Done |
| 9 | praisonai-bot boundary manifest | Done |

## Module ownership

| Module | Package | Owner |
|--------|---------|-------|
| `cli/main.py` | praisonai-code | Thin dispatcher + re-exports (≤300 lines) |
| `cli/legacy/env_security.py` | praisonai-code | Standalone-safe env validation |
| `cli/legacy/prompt_dispatch.py` | praisonai-code | Standalone `--help` |
| `cli/legacy/praison_ai.py` | praisonai-code | PraisonAI class shell (terminal/chat/run) |
| `cli/legacy/inbuilt_tools.py` | praisonai | Lazy loader helpers |
| `cli/legacy/framework_run.py` | praisonai | Framework adapter imports |
| `cli/legacy/subcommand_handlers.py` | praisonai | memory, rules, hooks, knowledge, docs, mcp, commit, context, research |
| `cli/legacy/workflow_commands.py` | praisonai | Workflow + YAML runner |
| `cli/legacy/direct_prompt.py` | praisonai | handle_direct_prompt + tool loaders |
| `cli/legacy/dispatch/argparse_builder.py` | praisonai | ArgumentParser schema |
| `cli/legacy/dispatch/legacy_dispatch.py` | praisonai | Plain dispatch dict |
| `cli/legacy/interactive_legacy.py` | praisonai | REPL/TUI (not deprecated) |

## Compatibility contract

- `praisonai.cli.main is praisonai_code.cli.main` — preserve `sys.modules` swap
- `OldPraisonAI is NewPraisonAI` — same class object
- Explicit re-exports on code `main`: `PraisonAI`, `_get_*`, `_fw_*`, `_validate_env_key`, availability flags
- 47 code-side lazy imports of `PraisonAI` must keep working

## Dead code removed (Phase 0)

| Branch | Reason |
|--------|--------|
| `serve` in special_commands block (~1719) | Dead — `_handle_serve_command` exits at 1383 |
| `call` in special_commands (~1452) | Dead — handled at 1423 |
| `replay` in special_commands (~1927) | Dead — handled at 1411 |
| `gateway`/`bot` legacy argparse (~1993) | Redirect to Typer; broken relative import |

## Typer-only commands (legacy gap)

- `github` — listed in `special_commands` but no parse_args handler; Typer canonical

## Two daemon concepts (do not merge)

| Command | Location | Purpose |
|---------|----------|---------|
| `praisonai daemon` | `praisonai_code/cli/commands/daemon.py` | Warm runtime |
| `praisonai.daemon` | `praisonai/daemon/systemd.py` | OS bot service (`praisonai-bot` unit) |

## Future praisonai-bot manifest (Phase 9)

Must move together when package splits:

- `praisonai/gateway/*`, `praisonai/bots/*`
- `praisonai/daemon/{systemd,launchd,windows}.py`
- `praisonai/integration/{gateway_host,host_app,bridges/}`
- `cli/features/{gateway,bots_cli,onboard,approval,serve}.py`
- `cli/commands/{bot,gateway,pairing,identity,onboard,kanban,claw,dashboard,mint_link}`
- SDK lazy re-exports in `praisonaiagents.gateway`

Stays in code: doctor checks (bridge targets), `_paths.resolve_bot_config_path`, warm runtime daemon.

## Gates

```bash
bash scripts/check_c7_imports.sh
python scripts/audit_hybrid_modules.py
pytest src/praisonai/tests/unit/test_c5_backward_compat.py
pytest src/praisonai/tests/unit/cli/test_main_dispatcher.py
pytest src/praisonai/tests/unit/cli/test_legacy_parse_args.py
pytest src/praisonai/tests/unit/cli/test_init_provider_guard.py
bash scripts/check_main_py_lines.sh
```

## Post-validation fixes (2026-07-03)

Multi-agent validation found and fixed:

- **P0:** 27 delegate methods were inside `if __name__` — moved onto `PraisonAI` class
- **P0:** `.features` → `..features` relative imports in `praison_ai.py` and wrapper legacy modules
- **P1:** `direct_prompt.py` missing availability/autogen imports — bridged via `praisonai_code.cli.main`
- **P1:** `workflow_commands.py` missing `_fw_workflow_module` import
- **P1:** `subcommand_handlers` commit sensitive-file refs (`self._SENSITIVE_*` → module constants)
- **P1:** `interactive_legacy` internal `self._helper()` → `_helper(self)` module calls
- **P1:** Duplicate `_provider_preflight_message` — `praison_ai.main()` now uses `cli.main` for patch compat
- **P2:** `prompt_dispatch.run_standalone_help()` wired in `praisonai_code.__main__` when wrapper absent
- **P2:** `_load_interactive_tools` and `_run_chat_mode` delegates added (found via real-API smoke)

## Real-API validation (2026-07-03)

Run with `source ~/.zshrc` then:

```bash
python scripts/c84_real_api_smoke.py          # 12/12 C8.4 paths
python scripts/c8_release_smoke_real_api.py   # 21/21 full tier smoke
RUN_REAL_KEY_TESTS=1 PRAISONAI_ALLOW_NETWORK=1 pytest src/praisonai/tests/integration/test_real_key_smoke.py
```

C8.4 smoke covers: module identity, `handle_direct_prompt` (both import paths), Typer run/code, legacy `-m praisonai.cli.main`, memory/workflow delegates, legacy YAML, import gates.
