{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "praisonai-agents-api.serviceAccountName" . }}
  labels:
    {{- include "praisonai-agents-api.labels" . | nindent 4 }}
{{- end }}
