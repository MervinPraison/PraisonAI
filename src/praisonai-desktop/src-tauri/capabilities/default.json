{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the webview is allowed to ask the shell to do.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "core:window:allow-start-dragging"
  ]
}
