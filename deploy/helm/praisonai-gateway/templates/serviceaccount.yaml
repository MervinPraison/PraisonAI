{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "praisonai-gateway.serviceAccountName" . }}
  labels:
    {{- include "praisonai-gateway.labels" . | nindent 4 }}
{{- end }}
