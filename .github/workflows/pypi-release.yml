name: PyPI Release

# Environments (Settings → Environments):
#   pypi-auto — patch releases from ci/nightly (no reviewers, main branch only)
#   pypi      — manual workflow_dispatch and major/minor (required reviewers)

on:
  workflow_dispatch:
    inputs:
      bump:
        description: 'Version bump type for all packages (agents, code, wrapper)'
        required: true
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major
      dry_run:
        description: 'Preview versions and steps without publishing or pushing'
        required: false
        default: false
        type: boolean
      skip_agents:
        description: 'Skip agents publish (recovery if already on PyPI)'
        required: false
        default: false
        type: boolean
      skip_wrapper:
        description: 'Skip wrapper publish (agents and/or code only)'
        required: false
        default: false
        type: boolean
      skip_code:
        description: 'Skip praisonai-code publish (recovery if already on PyPI)'
        required: false
        default: false
        type: boolean
      skip_bot:
        description: 'Skip praisonai-bot publish (recovery if already on PyPI)'
        required: false
        default: false
        type: boolean
      skip_train:
        description: 'Skip praisonai-train publish (recovery if already on PyPI)'
        required: false
        default: false
        type: boolean
      skip_browser:
        description: 'Skip praisonai-browser publish (recovery if already on PyPI)'
        required: false
        default: false
        type: boolean
      skip_mcp:
        description: 'Skip praisonai-mcp publish (recovery if already on PyPI)'
        required: false
        default: false
        type: boolean
      agents_version:
        description: 'Override agents version (empty = auto bump from pyproject.toml)'
        required: false
        default: ''
        type: string
      wrapper_version:
        description: 'Override wrapper version (empty = auto bump from version.py)'
        required: false
        default: ''
        type: string
      code_version:
        description: 'Override praisonai-code version (empty = auto bump from pyproject.toml)'
        required: false
        default: ''
        type: string
      bot_version:
        description: 'Override praisonai-bot version (empty = auto bump from pyproject.toml)'
        required: false
        default: ''
        type: string
      train_version:
        description: 'Override praisonai-train version (empty = auto bump from pyproject.toml)'
        required: false
        default: ''
        type: string
      browser_version:
        description: 'Override praisonai-browser version (empty = auto bump from pyproject.toml)'
        required: false
        default: ''
        type: string
      mcp_version:
        description: 'Override praisonai-mcp version (empty = auto bump from pyproject.toml)'
        required: false
        default: ''
        type: string
      source:
        description: 'Trigger source (audit trail)'
        required: false
        default: manual
        type: choice
        options:
          - manual
          - nightly
          - ci
      trigger_sha:
        description: 'Main commit SHA that passed release gate (audit/dedupe)'
        required: false
        default: ''
        type: string

permissions:
  contents: write

concurrency:
  group: pypi-release
  cancel-in-progress: false

jobs:
  release:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: >-
      ${{
        (inputs.source == 'ci' || inputs.source == 'nightly') &&
        inputs.bump == 'patch' &&
        'pypi-auto' || 'pypi'
      }}
    env:
      UV_PUBLISH_TOKEN: ${{ secrets.PYPI_TOKEN || secrets.PYPI_API_TOKEN }}
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
    steps:
      - name: Checkout main
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with:
          ref: main
          fetch-depth: 0
          token: ${{ secrets.GH_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: '3.11'

      - name: Set up uv
        uses: astral-sh/setup-uv@38f3f104447c67c051c4a08e39b64a148898af3a  # v4

      - name: Configure git
        run: |
          git config user.name "MervinPraison"
          git config user.email "454862+MervinPraison@users.noreply.github.com"
          gh auth setup-git

      - name: Release summary
        run: |
          echo "Source: ${{ inputs.source }}"
          echo "Trigger SHA: ${{ inputs.trigger_sha }}"
          echo "Bump:   ${{ inputs.bump }}"
          echo "Dry run: ${{ inputs.dry_run }}"
          echo "Environment: ${{ (inputs.source == 'ci' || inputs.source == 'nightly') && inputs.bump == 'patch' && 'pypi-auto' || 'pypi' }}"

      - name: Compute release versions
        id: versions
        env:
          BUMP: ${{ inputs.bump }}
          AGENTS_OVERRIDE: ${{ inputs.agents_version }}
          WRAPPER_OVERRIDE: ${{ inputs.wrapper_version }}
          CODE_OVERRIDE: ${{ inputs.code_version }}
          BOT_OVERRIDE: ${{ inputs.bot_version }}
          TRAIN_OVERRIDE: ${{ inputs.train_version }}
          BROWSER_OVERRIDE: ${{ inputs.browser_version }}
          MCP_OVERRIDE: ${{ inputs.mcp_version }}
          SKIP_AGENTS: ${{ inputs.skip_agents }}
          SKIP_CODE: ${{ inputs.skip_code }}
          SKIP_BOT: ${{ inputs.skip_bot }}
          SKIP_TRAIN: ${{ inputs.skip_train }}
          SKIP_BROWSER: ${{ inputs.skip_browser }}
          SKIP_MCP: ${{ inputs.skip_mcp }}
        run: |
          python <<'PY'
          import os
          import re
          import sys
          from pathlib import Path

          SEMVER = re.compile(r"^\d+\.\d+\.\d+$")
          root = Path(".")
          bump = os.environ["BUMP"]

          def bump_version(version: str) -> str:
              parts = version.split(".")
              if len(parts) != 3:
                  print(f"Invalid version: {version}", file=sys.stderr)
                  sys.exit(1)
              maj, min_, patch = (int(parts[0]), int(parts[1]), int(parts[2]))
              if bump == "major":
                  return f"{maj + 1}.0.0"
              if bump == "minor":
                  return f"{maj}.{min_ + 1}.0"
              return f"{maj}.{min_}.{patch + 1}"

          def validate_version(label: str, value: str) -> None:
              if value and not SEMVER.match(value):
                  print(f"Invalid {label} override: {value!r} (expected X.Y.Z)", file=sys.stderr)
                  sys.exit(1)

          agents_override = os.environ.get("AGENTS_OVERRIDE", "").strip()
          wrapper_override = os.environ.get("WRAPPER_OVERRIDE", "").strip()
          code_override = os.environ.get("CODE_OVERRIDE", "").strip()
          bot_override = os.environ.get("BOT_OVERRIDE", "").strip()
          train_override = os.environ.get("TRAIN_OVERRIDE", "").strip()
          browser_override = os.environ.get("BROWSER_OVERRIDE", "").strip()
          mcp_override = os.environ.get("MCP_OVERRIDE", "").strip()
          validate_version("agents_version", agents_override)
          validate_version("wrapper_version", wrapper_override)
          validate_version("code_version", code_override)
          validate_version("bot_version", bot_override)
          validate_version("train_version", train_override)
          validate_version("browser_version", browser_override)
          validate_version("mcp_version", mcp_override)

          agents_pyproject = root / "src/praisonai-agents/pyproject.toml"
          agents_content = agents_pyproject.read_text()
          agents_match = re.search(r'^version\s*=\s*"([^"]+)"', agents_content, re.MULTILINE)
          if not agents_match:
              print("Could not read agents version from pyproject.toml", file=sys.stderr)
              sys.exit(1)
          current_agents = agents_match.group(1)

          code_pyproject = root / "src/praisonai-code/pyproject.toml"
          code_content = code_pyproject.read_text()
          code_match = re.search(r'^version\s*=\s*"([^"]+)"', code_content, re.MULTILINE)
          if not code_match:
              print("Could not read praisonai-code version from pyproject.toml", file=sys.stderr)
              sys.exit(1)
          current_code = code_match.group(1)

          bot_pyproject = root / "src/praisonai-bot/pyproject.toml"
          bot_content = bot_pyproject.read_text()
          bot_match = re.search(r'^version\s*=\s*"([^"]+)"', bot_content, re.MULTILINE)
          if not bot_match:
              print("Could not read praisonai-bot version from pyproject.toml", file=sys.stderr)
              sys.exit(1)
          current_bot = bot_match.group(1)

          train_pyproject = root / "src/praisonai-train/pyproject.toml"
          train_content = train_pyproject.read_text()
          train_match = re.search(r'^version\s*=\s*"([^"]+)"', train_content, re.MULTILINE)
          if not train_match:
              print("Could not read praisonai-train version from pyproject.toml", file=sys.stderr)
              sys.exit(1)
          current_train = train_match.group(1)

          browser_pyproject = root / "src/praisonai-browser/pyproject.toml"
          browser_content = browser_pyproject.read_text()
          browser_match = re.search(r'^version\s*=\s*"([^"]+)"', browser_content, re.MULTILINE)
          if not browser_match:
              print("Could not read praisonai-browser version from pyproject.toml", file=sys.stderr)
              sys.exit(1)
          current_browser = browser_match.group(1)

          mcp_pyproject = root / "src/praisonai-mcp/pyproject.toml"
          mcp_content = mcp_pyproject.read_text()
          mcp_match = re.search(r'^version\s*=\s*"([^"]+)"', mcp_content, re.MULTILINE)
          if not mcp_match:
              print("Could not read praisonai-mcp version from pyproject.toml", file=sys.stderr)
              sys.exit(1)
          current_mcp = mcp_match.group(1)

          wrapper_version_py = root / "src/praisonai/praisonai/version.py"
          wrapper_content = wrapper_version_py.read_text()
          wrapper_match = re.search(r'__version__ = "([^"]+)"', wrapper_content)
          if not wrapper_match:
              print("Could not read wrapper version from version.py", file=sys.stderr)
              sys.exit(1)
          current_wrapper = wrapper_match.group(1)

          for label, value in (
              ("agents", current_agents),
              ("code", current_code),
              ("bot", current_bot),
              ("train", current_train),
              ("browser", current_browser),
              ("mcp", current_mcp),
              ("wrapper", current_wrapper),
          ):
              if not SEMVER.match(value):
                  print(f"Invalid current {label} version: {value}", file=sys.stderr)
                  sys.exit(1)

          skip_agents = os.environ.get("SKIP_AGENTS", "false").lower() == "true"
          skip_code = os.environ.get("SKIP_CODE", "false").lower() == "true"
          skip_bot = os.environ.get("SKIP_BOT", "false").lower() == "true"
          skip_train = os.environ.get("SKIP_TRAIN", "false").lower() == "true"
          skip_browser = os.environ.get("SKIP_BROWSER", "false").lower() == "true"
          skip_mcp = os.environ.get("SKIP_MCP", "false").lower() == "true"

          if agents_override:
              agents_version = agents_override
          elif skip_agents:
              agents_version = current_agents
          else:
              agents_version = bump_version(current_agents)

          if code_override:
              code_version = code_override
          elif skip_code:
              code_version = current_code
          else:
              code_version = bump_version(current_code)

          if bot_override:
              bot_version = bot_override
          elif skip_bot:
              bot_version = current_bot
          else:
              bot_version = bump_version(current_bot)

          if train_override:
              train_version = train_override
          elif skip_train:
              train_version = current_train
          else:
              train_version = bump_version(current_train)

          if browser_override:
              browser_version = browser_override
          elif skip_browser:
              browser_version = current_browser
          else:
              browser_version = bump_version(current_browser)

          if mcp_override:
              mcp_version = mcp_override
          elif skip_mcp:
              mcp_version = current_mcp
          else:
              mcp_version = bump_version(current_mcp)

          if wrapper_override:
              wrapper_version = wrapper_override
          else:
              wrapper_version = bump_version(current_wrapper)

          github_output = os.environ["GITHUB_OUTPUT"]
          with open(github_output, "a", encoding="utf-8") as fh:
              fh.write(f"agents_version={agents_version}\n")
              fh.write(f"code_version={code_version}\n")
              fh.write(f"bot_version={bot_version}\n")
              fh.write(f"train_version={train_version}\n")
              fh.write(f"browser_version={browser_version}\n")
              fh.write(f"mcp_version={mcp_version}\n")
              fh.write(f"wrapper_version={wrapper_version}\n")
              fh.write(f"current_agents={current_agents}\n")
              fh.write(f"current_code={current_code}\n")
              fh.write(f"current_bot={current_bot}\n")
              fh.write(f"current_train={current_train}\n")
              fh.write(f"current_browser={current_browser}\n")
              fh.write(f"current_mcp={current_mcp}\n")
              fh.write(f"current_wrapper={current_wrapper}\n")

          print(f"Agents:  {current_agents} -> {agents_version}")
          print(f"Code:    {current_code} -> {code_version}")
          print(f"Bot:     {current_bot} -> {bot_version}")
          print(f"Train:   {current_train} -> {train_version}")
          print(f"Browser: {current_browser} -> {browser_version}")
          print(f"MCP:     {current_mcp} -> {mcp_version}")
          print(f"Wrapper: {current_wrapper} -> {wrapper_version}")
          PY

      - name: Detect existing PyPI versions
        id: pypi_exists
        if: inputs.dry_run != true
        env:
          AGENTS_VERSION: ${{ steps.versions.outputs.agents_version }}
          CODE_VERSION: ${{ steps.versions.outputs.code_version }}
          BOT_VERSION: ${{ steps.versions.outputs.bot_version }}
          TRAIN_VERSION: ${{ steps.versions.outputs.train_version }}
          BROWSER_VERSION: ${{ steps.versions.outputs.browser_version }}
          MCP_VERSION: ${{ steps.versions.outputs.mcp_version }}
          WRAPPER_VERSION: ${{ steps.versions.outputs.wrapper_version }}
          INPUT_SKIP_AGENTS: ${{ inputs.skip_agents }}
          INPUT_SKIP_CODE: ${{ inputs.skip_code }}
          INPUT_SKIP_BOT: ${{ inputs.skip_bot }}
          INPUT_SKIP_TRAIN: ${{ inputs.skip_train }}
          INPUT_SKIP_BROWSER: ${{ inputs.skip_browser }}
          INPUT_SKIP_MCP: ${{ inputs.skip_mcp }}
          INPUT_SKIP_WRAPPER: ${{ inputs.skip_wrapper }}
        run: |
          set -euo pipefail
          skip_agents="${INPUT_SKIP_AGENTS}"
          skip_code="${INPUT_SKIP_CODE}"
          skip_bot="${INPUT_SKIP_BOT}"
          skip_train="${INPUT_SKIP_TRAIN}"
          skip_browser="${INPUT_SKIP_BROWSER}"
          skip_mcp="${INPUT_SKIP_MCP}"
          skip_wrapper="${INPUT_SKIP_WRAPPER}"
          if [ "$skip_agents" != "true" ]; then
            curl -fsSL "https://pypi.org/pypi/praisonaiagents/${AGENTS_VERSION}/json" >/dev/null && skip_agents=true || true
          fi
          if [ "$skip_code" != "true" ]; then
            curl -fsSL "https://pypi.org/pypi/praisonai-code/${CODE_VERSION}/json" >/dev/null && skip_code=true || true
          fi
          if [ "$skip_bot" != "true" ]; then
            curl -fsSL "https://pypi.org/pypi/praisonai-bot/${BOT_VERSION}/json" >/dev/null && skip_bot=true || true
          fi
          if [ "$skip_train" != "true" ]; then
            curl -fsSL "https://pypi.org/pypi/praisonai-train/${TRAIN_VERSION}/json" >/dev/null && skip_train=true || true
          fi
          if [ "$skip_browser" != "true" ]; then
            curl -fsSL "https://pypi.org/pypi/praisonai-browser/${BROWSER_VERSION}/json" >/dev/null && skip_browser=true || true
          fi
          if [ "$skip_mcp" != "true" ]; then
            curl -fsSL "https://pypi.org/pypi/praisonai-mcp/${MCP_VERSION}/json" >/dev/null && skip_mcp=true || true
          fi
          if [ "$skip_wrapper" != "true" ]; then
            curl -fsSL "https://pypi.org/pypi/praisonai/${WRAPPER_VERSION}/json" >/dev/null && skip_wrapper=true || true
          fi
          echo "skip_agents=${skip_agents}" >> "$GITHUB_OUTPUT"
          echo "skip_code=${skip_code}" >> "$GITHUB_OUTPUT"
          echo "skip_bot=${skip_bot}" >> "$GITHUB_OUTPUT"
          echo "skip_train=${skip_train}" >> "$GITHUB_OUTPUT"
          echo "skip_browser=${skip_browser}" >> "$GITHUB_OUTPUT"
          echo "skip_mcp=${skip_mcp}" >> "$GITHUB_OUTPUT"
          echo "skip_wrapper=${skip_wrapper}" >> "$GITHUB_OUTPUT"
          if [ "$skip_agents" = "true" ] && [ "$skip_code" = "true" ] && [ "$skip_bot" = "true" ] && [ "$skip_train" = "true" ] && [ "$skip_browser" = "true" ] && [ "$skip_mcp" = "true" ] && [ "$skip_wrapper" = "true" ]; then
            echo "All target versions already on PyPI — nothing to publish."
          fi

      - name: Exit if already published
        if: >-
          inputs.dry_run != true &&
          steps.pypi_exists.outputs.skip_agents == 'true' &&
          steps.pypi_exists.outputs.skip_code == 'true' &&
          steps.pypi_exists.outputs.skip_bot == 'true' &&
          steps.pypi_exists.outputs.skip_train == 'true' &&
          steps.pypi_exists.outputs.skip_browser == 'true' &&
          steps.pypi_exists.outputs.skip_mcp == 'true' &&
          steps.pypi_exists.outputs.skip_wrapper == 'true'
        run: echo "Idempotent skip — target versions already on PyPI."

      - name: Dry run summary
        if: inputs.dry_run
        run: |
          echo "Dry run — no publish, commit, or push."
          echo "Agents version:  ${{ steps.versions.outputs.agents_version }}"
          echo "Code version:    ${{ steps.versions.outputs.code_version }}"
          echo "Bot version:     ${{ steps.versions.outputs.bot_version }}"
          echo "Train version:   ${{ steps.versions.outputs.train_version }}"
          echo "Browser version: ${{ steps.versions.outputs.browser_version }}"
          echo "MCP version:     ${{ steps.versions.outputs.mcp_version }}"
          echo "Wrapper version: ${{ steps.versions.outputs.wrapper_version }}"
          echo ""
          if [ "${{ inputs.skip_agents }}" != "true" ]; then
            echo "Would publish praisonaiagents ${{ steps.versions.outputs.agents_version }}"
          fi
          if [ "${{ inputs.skip_code }}" != "true" ]; then
            echo "Would publish praisonai-code ${{ steps.versions.outputs.code_version }}"
          fi
          if [ "${{ inputs.skip_bot }}" != "true" ]; then
            echo "Would publish praisonai-bot ${{ steps.versions.outputs.bot_version }}"
          fi
          if [ "${{ inputs.skip_train }}" != "true" ]; then
            echo "Would publish praisonai-train ${{ steps.versions.outputs.train_version }}"
          fi
          if [ "${{ inputs.skip_browser }}" != "true" ]; then
            echo "Would publish praisonai-browser ${{ steps.versions.outputs.browser_version }}"
          fi
          if [ "${{ inputs.skip_mcp }}" != "true" ]; then
            echo "Would publish praisonai-mcp ${{ steps.versions.outputs.mcp_version }}"
          fi
          if [ "${{ inputs.skip_wrapper }}" != "true" ]; then
            echo "Would run bump_and_release.py ${{ steps.versions.outputs.wrapper_version }} --agents ${{ steps.versions.outputs.agents_version }} --code-pin ${{ steps.versions.outputs.code_version }} --bot-pin ${{ steps.versions.outputs.bot_version }} --train-pin ${{ steps.versions.outputs.train_version }} --browser-pin ${{ steps.versions.outputs.browser_version }} --mcp-pin ${{ steps.versions.outputs.mcp_version }}"
            echo "Would uv publish praisonai ${{ steps.versions.outputs.wrapper_version }}"
          fi

      - name: Publish praisonaiagents
        if: >-
          inputs.dry_run != true &&
          inputs.skip_agents != true &&
          steps.pypi_exists.outputs.skip_agents != 'true'
        working-directory: src/praisonai-agents
        env:
          NEW_VERSION: ${{ steps.versions.outputs.agents_version }}
          CURRENT: ${{ steps.versions.outputs.current_agents }}
        run: |
          set -euo pipefail
          python <<'PY'
          import os
          from pathlib import Path
          path = Path("pyproject.toml")
          content = path.read_text()
          current = os.environ["CURRENT"]
          new = os.environ["NEW_VERSION"]
          path.write_text(content.replace(f'version = "{current}"', f'version = "{new}"', 1))
          PY

          rm -rf dist
          uv lock
          uv build
          uv publish

      - name: Wait for praisonaiagents on PyPI
        if: >-
          inputs.dry_run != true &&
          inputs.skip_agents != true &&
          steps.pypi_exists.outputs.skip_agents != 'true'
        run: |
          set -euo pipefail
          PACKAGE="praisonaiagents"
          VERSION="${{ steps.versions.outputs.agents_version }}"
          MAX_WAIT=600
          INTERVAL=30
          START=$(date +%s)

          while true; do
            if curl -fsSL "https://pypi.org/pypi/${PACKAGE}/${VERSION}/json" >/dev/null; then
              echo "✅ ${PACKAGE}==${VERSION} is available on PyPI"
              exit 0
            fi
            ELAPSED=$(( $(date +%s) - START ))
            if [ "$ELAPSED" -ge "$MAX_WAIT" ]; then
              echo "❌ Timeout waiting for ${PACKAGE}==${VERSION} on PyPI"
              exit 1
            fi
            echo "⏳ Waiting for ${PACKAGE}==${VERSION} ($(( MAX_WAIT - ELAPSED ))s remaining...)"
            sleep "$INTERVAL"
          done

      - name: Commit agents version bump
        if: >-
          inputs.dry_run != true &&
          inputs.skip_agents != true &&
          steps.pypi_exists.outputs.skip_agents != 'true'
        run: |
          set -euo pipefail
          git add src/praisonai-agents/pyproject.toml src/praisonai-agents/uv.lock
          git diff --cached --quiet && { echo "No agents files to commit"; exit 0; }
          git commit -m "Bump praisonaiagents to ${{ steps.versions.outputs.agents_version }}"
          git pull --rebase origin main
          git push origin main

      - name: Publish praisonai-code
        if: >-
          inputs.dry_run != true &&
          inputs.skip_code != true &&
          steps.pypi_exists.outputs.skip_code != 'true'
        working-directory: src/praisonai-code
        env:
          NEW_VERSION: ${{ steps.versions.outputs.code_version }}
          CURRENT: ${{ steps.versions.outputs.current_code }}
        run: |
          set -euo pipefail
          python <<'PY'
          import os
          import re
          from pathlib import Path
          new = os.environ["NEW_VERSION"]
          current = os.environ["CURRENT"]
          pyproject = Path("pyproject.toml")
          content = pyproject.read_text()
          pyproject.write_text(content.replace(f'version = "{current}"', f'version = "{new}"', 1))
          init_py = Path("praisonai_code/__init__.py")
          init_content = init_py.read_text()
          init_py.write_text(re.sub(r'__version__ = "[^"]+"', f'__version__ = "{new}"', init_content, count=1))
          PY

          rm -rf dist
          uv lock
          uv build
          uv publish

      - name: Wait for praisonai-code on PyPI
        if: >-
          inputs.dry_run != true &&
          inputs.skip_code != true &&
          steps.pypi_exists.outputs.skip_code != 'true'
        run: |
          set -euo pipefail
          PACKAGE="praisonai-code"
          VERSION="${{ steps.versions.outputs.code_version }}"
          MAX_WAIT=600
          INTERVAL=30
          START=$(date +%s)

          while true; do
            if curl -fsSL "https://pypi.org/pypi/${PACKAGE}/${VERSION}/json" >/dev/null; then
              echo "✅ ${PACKAGE}==${VERSION} is available on PyPI"
              exit 0
            fi
            ELAPSED=$(( $(date +%s) - START ))
            if [ "$ELAPSED" -ge "$MAX_WAIT" ]; then
              echo "❌ Timeout waiting for ${PACKAGE}==${VERSION} on PyPI"
              exit 1
            fi
            echo "⏳ Waiting for ${PACKAGE}==${VERSION} ($(( MAX_WAIT - ELAPSED ))s remaining...)"
            sleep "$INTERVAL"
          done

      - name: Commit praisonai-code version bump
        if: >-
          inputs.dry_run != true &&
          inputs.skip_code != true &&
          steps.pypi_exists.outputs.skip_code != 'true'
        run: |
          set -euo pipefail
          git add src/praisonai-code/pyproject.toml src/praisonai-code/uv.lock src/praisonai-code/praisonai_code/__init__.py
          git diff --cached --quiet && { echo "No code files to commit"; exit 0; }
          git commit -m "Bump praisonai-code to ${{ steps.versions.outputs.code_version }}"
          git pull --rebase origin main
          git push origin main

      - name: Publish praisonai-bot
        if: >-
          inputs.dry_run != true &&
          inputs.skip_bot != true &&
          steps.pypi_exists.outputs.skip_bot != 'true'
        working-directory: src/praisonai-bot
        env:
          NEW_VERSION: ${{ steps.versions.outputs.bot_version }}
          CURRENT: ${{ steps.versions.outputs.current_bot }}
        run: |
          set -euo pipefail
          python <<'PY'
          import os
          import re
          from pathlib import Path
          new = os.environ["NEW_VERSION"]
          current = os.environ["CURRENT"]
          pyproject = Path("pyproject.toml")
          content = pyproject.read_text()
          pyproject.write_text(content.replace(f'version = "{current}"', f'version = "{new}"', 1))
          version_py = Path("praisonai_bot/_version.py")
          version_content = version_py.read_text()
          version_py.write_text(re.sub(r'__version__ = "[^"]+"', f'__version__ = "{new}"', version_content, count=1))
          PY

          rm -rf dist
          uv lock
          uv build
          uv publish

      - name: Wait for praisonai-bot on PyPI
        if: >-
          inputs.dry_run != true &&
          inputs.skip_bot != true &&
          steps.pypi_exists.outputs.skip_bot != 'true'
        run: |
          set -euo pipefail
          PACKAGE="praisonai-bot"
          VERSION="${{ steps.versions.outputs.bot_version }}"
          MAX_WAIT=600
          INTERVAL=30
          START=$(date +%s)

          while true; do
            if curl -fsSL "https://pypi.org/pypi/${PACKAGE}/${VERSION}/json" >/dev/null; then
              echo "✅ ${PACKAGE}==${VERSION} is available on PyPI"
              exit 0
            fi
            ELAPSED=$(( $(date +%s) - START ))
            if [ "$ELAPSED" -ge "$MAX_WAIT" ]; then
              echo "❌ Timeout waiting for ${PACKAGE}==${VERSION} on PyPI"
              exit 1
            fi
            echo "⏳ Waiting for ${PACKAGE}==${VERSION} ($(( MAX_WAIT - ELAPSED ))s remaining...)"
            sleep "$INTERVAL"
          done

      - name: Commit praisonai-bot version bump
        if: >-
          inputs.dry_run != true &&
          inputs.skip_bot != true &&
          steps.pypi_exists.outputs.skip_bot != 'true'
        run: |
          set -euo pipefail
          git add src/praisonai-bot/pyproject.toml src/praisonai-bot/uv.lock src/praisonai-bot/praisonai_bot/_version.py
          git diff --cached --quiet && { echo "No bot files to commit"; exit 0; }
          git commit -m "Bump praisonai-bot to ${{ steps.versions.outputs.bot_version }}"
          git pull --rebase origin main
          git push origin main

      - name: Publish praisonai-train
        if: >-
          inputs.dry_run != true &&
          inputs.skip_train != true &&
          steps.pypi_exists.outputs.skip_train != 'true'
        working-directory: src/praisonai-train
        env:
          NEW_VERSION: ${{ steps.versions.outputs.train_version }}
          CURRENT: ${{ steps.versions.outputs.current_train }}
        run: |
          set -euo pipefail
          python <<'PY'
          import os
          import re
          from pathlib import Path
          new = os.environ["NEW_VERSION"]
          current = os.environ["CURRENT"]
          pyproject = Path("pyproject.toml")
          content = pyproject.read_text()
          pyproject.write_text(content.replace(f'version = "{current}"', f'version = "{new}"', 1))
          version_py = Path("praisonai_train/_version.py")
          version_content = version_py.read_text()
          version_py.write_text(re.sub(r'__version__ = "[^"]+"', f'__version__ = "{new}"', version_content, count=1))
          PY

          rm -rf dist
          uv lock
          uv build
          uv publish

      - name: Wait for praisonai-train on PyPI
        if: >-
          inputs.dry_run != true &&
          inputs.skip_train != true &&
          steps.pypi_exists.outputs.skip_train != 'true'
        run: |
          set -euo pipefail
          PACKAGE="praisonai-train"
          VERSION="${{ steps.versions.outputs.train_version }}"
          MAX_WAIT=600
          INTERVAL=30
          START=$(date +%s)

          while true; do
            if curl -fsSL "https://pypi.org/pypi/${PACKAGE}/${VERSION}/json" >/dev/null; then
              echo "✅ ${PACKAGE}==${VERSION} is available on PyPI"
              exit 0
            fi
            ELAPSED=$(( $(date +%s) - START ))
            if [ "$ELAPSED" -ge "$MAX_WAIT" ]; then
              echo "❌ Timeout waiting for ${PACKAGE}==${VERSION} on PyPI"
              exit 1
            fi
            echo "⏳ Waiting for ${PACKAGE}==${VERSION} ($(( MAX_WAIT - ELAPSED ))s remaining...)"
            sleep "$INTERVAL"
          done

      - name: Commit praisonai-train version bump
        if: >-
          inputs.dry_run != true &&
          inputs.skip_train != true &&
          steps.pypi_exists.outputs.skip_train != 'true'
        run: |
          set -euo pipefail
          git add src/praisonai-train/pyproject.toml src/praisonai-train/uv.lock src/praisonai-train/praisonai_train/_version.py
          git diff --cached --quiet && { echo "No train files to commit"; exit 0; }
          git commit -m "Bump praisonai-train to ${{ steps.versions.outputs.train_version }}"
          git pull --rebase origin main
          git push origin main

      - name: Publish praisonai-browser
        if: >-
          inputs.dry_run != true &&
          inputs.skip_browser != true &&
          steps.pypi_exists.outputs.skip_browser != 'true'
        working-directory: src/praisonai-browser
        env:
          NEW_VERSION: ${{ steps.versions.outputs.browser_version }}
          CURRENT: ${{ steps.versions.outputs.current_browser }}
        run: |
          set -euo pipefail
          python <<'PY'
          import os
          import re
          from pathlib import Path
          new = os.environ["NEW_VERSION"]
          current = os.environ["CURRENT"]
          pyproject = Path("pyproject.toml")
          content = pyproject.read_text()
          pyproject.write_text(content.replace(f'version = "{current}"', f'version = "{new}"', 1))
          version_py = Path("praisonai_browser/_version.py")
          version_content = version_py.read_text()
          version_py.write_text(re.sub(r'__version__ = "[^"]+"', f'__version__ = "{new}"', version_content, count=1))
          PY

          rm -rf dist
          uv lock
          uv build
          uv publish

      - name: Wait for praisonai-browser on PyPI
        if: >-
          inputs.dry_run != true &&
          inputs.skip_browser != true &&
          steps.pypi_exists.outputs.skip_browser != 'true'
        run: |
          set -euo pipefail
          PACKAGE="praisonai-browser"
          VERSION="${{ steps.versions.outputs.browser_version }}"
          MAX_WAIT=600
          INTERVAL=30
          START=$(date +%s)

          while true; do
            if curl -fsSL "https://pypi.org/pypi/${PACKAGE}/${VERSION}/json" >/dev/null; then
              echo "✅ ${PACKAGE}==${VERSION} is available on PyPI"
              exit 0
            fi
            ELAPSED=$(( $(date +%s) - START ))
            if [ "$ELAPSED" -ge "$MAX_WAIT" ]; then
              echo "❌ Timeout waiting for ${PACKAGE}==${VERSION} on PyPI"
              exit 1
            fi
            echo "⏳ Waiting for ${PACKAGE}==${VERSION} ($(( MAX_WAIT - ELAPSED ))s remaining...)"
            sleep "$INTERVAL"
          done

      - name: Commit praisonai-browser version bump
        if: >-
          inputs.dry_run != true &&
          inputs.skip_browser != true &&
          steps.pypi_exists.outputs.skip_browser != 'true'
        run: |
          set -euo pipefail
          git add src/praisonai-browser/pyproject.toml src/praisonai-browser/uv.lock src/praisonai-browser/praisonai_browser/_version.py
          git diff --cached --quiet && { echo "No browser files to commit"; exit 0; }
          git commit -m "Bump praisonai-browser to ${{ steps.versions.outputs.browser_version }}"
          git pull --rebase origin main
          git push origin main

      - name: Publish praisonai-mcp
        if: >-
          inputs.dry_run != true &&
          inputs.skip_mcp != true &&
          steps.pypi_exists.outputs.skip_mcp != 'true'
        working-directory: src/praisonai-mcp
        env:
          NEW_VERSION: ${{ steps.versions.outputs.mcp_version }}
          CURRENT: ${{ steps.versions.outputs.current_mcp }}
        run: |
          set -euo pipefail
          python <<'PY'
          import os
          import re
          from pathlib import Path
          new = os.environ["NEW_VERSION"]
          current = os.environ["CURRENT"]
          pyproject = Path("pyproject.toml")
          content = pyproject.read_text()
          pyproject.write_text(content.replace(f'version = "{current}"', f'version = "{new}"', 1))
          version_py = Path("praisonai_mcp/_version.py")
          version_content = version_py.read_text()
          version_py.write_text(re.sub(r'__version__ = "[^"]+"', f'__version__ = "{new}"', version_content, count=1))
          PY

          rm -rf dist
          uv lock
          uv build
          uv publish

      - name: Wait for praisonai-mcp on PyPI
        if: >-
          inputs.dry_run != true &&
          inputs.skip_mcp != true &&
          steps.pypi_exists.outputs.skip_mcp != 'true'
        run: |
          set -euo pipefail
          PACKAGE="praisonai-mcp"
          VERSION="${{ steps.versions.outputs.mcp_version }}"
          MAX_WAIT=600
          INTERVAL=30
          START=$(date +%s)

          while true; do
            if curl -fsSL "https://pypi.org/pypi/${PACKAGE}/${VERSION}/json" >/dev/null; then
              echo "✅ ${PACKAGE}==${VERSION} is available on PyPI"
              exit 0
            fi
            ELAPSED=$(( $(date +%s) - START ))
            if [ "$ELAPSED" -ge "$MAX_WAIT" ]; then
              echo "❌ Timeout waiting for ${PACKAGE}==${VERSION} on PyPI"
              exit 1
            fi
            echo "⏳ Waiting for ${PACKAGE}==${VERSION} ($(( MAX_WAIT - ELAPSED ))s remaining...)"
            sleep "$INTERVAL"
          done

      - name: Commit praisonai-mcp version bump
        if: >-
          inputs.dry_run != true &&
          inputs.skip_mcp != true &&
          steps.pypi_exists.outputs.skip_mcp != 'true'
        run: |
          set -euo pipefail
          git add src/praisonai-mcp/pyproject.toml src/praisonai-mcp/uv.lock src/praisonai-mcp/praisonai_mcp/_version.py
          git diff --cached --quiet && { echo "No mcp files to commit"; exit 0; }
          git commit -m "Bump praisonai-mcp to ${{ steps.versions.outputs.mcp_version }}"
          git pull --rebase origin main
          git push origin main

      - name: Bump and release wrapper
        if: >-
          inputs.dry_run != true &&
          inputs.skip_wrapper != true &&
          steps.pypi_exists.outputs.skip_wrapper != 'true'
        run: |
          set -euo pipefail
          python src/praisonai/scripts/bump_and_release.py "${{ steps.versions.outputs.wrapper_version }}" \
            --agents "${{ steps.versions.outputs.agents_version }}" \
            --code-pin "${{ steps.versions.outputs.code_version }}" \
            --bot-pin "${{ steps.versions.outputs.bot_version }}" \
            --train-pin "${{ steps.versions.outputs.train_version }}" \
            --browser-pin "${{ steps.versions.outputs.browser_version }}" \
            --mcp-pin "${{ steps.versions.outputs.mcp_version }}" \
            --wait --max-wait 600 --no-add-all

      - name: Publish praisonai wrapper to PyPI
        if: >-
          inputs.dry_run != true &&
          inputs.skip_wrapper != true &&
          steps.pypi_exists.outputs.skip_wrapper != 'true'
        working-directory: src/praisonai
        run: uv publish

      - name: Wait for praisonai on PyPI
        if: >-
          inputs.dry_run != true &&
          inputs.skip_wrapper != true &&
          steps.pypi_exists.outputs.skip_wrapper != 'true'
        run: |
          set -euo pipefail
          PACKAGE="praisonai"
          VERSION="${{ steps.versions.outputs.wrapper_version }}"
          MAX_WAIT=600
          INTERVAL=30
          START=$(date +%s)

          while true; do
            if curl -fsSL "https://pypi.org/pypi/${PACKAGE}/${VERSION}/json" >/dev/null; then
              echo "✅ ${PACKAGE}==${VERSION} is available on PyPI"
              exit 0
            fi
            ELAPSED=$(( $(date +%s) - START ))
            if [ "$ELAPSED" -ge "$MAX_WAIT" ]; then
              echo "❌ Timeout waiting for ${PACKAGE}==${VERSION} on PyPI"
              exit 1
            fi
            echo "⏳ Waiting for ${PACKAGE}==${VERSION} ($(( MAX_WAIT - ELAPSED ))s remaining...)"
            sleep "$INTERVAL"
          done

      - name: Verify PyPI versions
        if: inputs.dry_run != true
        run: |
          set -euo pipefail
          AGENTS="${{ steps.versions.outputs.agents_version }}"
          CODE="${{ steps.versions.outputs.code_version }}"
          BOT="${{ steps.versions.outputs.bot_version }}"
          TRAIN="${{ steps.versions.outputs.train_version }}"
          BROWSER="${{ steps.versions.outputs.browser_version }}"
          MCP="${{ steps.versions.outputs.mcp_version }}"
          WRAPPER="${{ steps.versions.outputs.wrapper_version }}"

          if [ "${{ inputs.skip_agents }}" != "true" ]; then
            echo "✅ praisonaiagents==${AGENTS}"
          fi

          if [ "${{ inputs.skip_code }}" != "true" ]; then
            echo "✅ praisonai-code==${CODE}"
          fi

          if [ "${{ inputs.skip_bot }}" != "true" ]; then
            echo "✅ praisonai-bot==${BOT}"
          fi

          if [ "${{ inputs.skip_train }}" != "true" ]; then
            echo "✅ praisonai-train==${TRAIN}"
          fi

          if [ "${{ inputs.skip_browser }}" != "true" ]; then
            echo "✅ praisonai-browser==${BROWSER}"
          fi

          if [ "${{ inputs.skip_mcp }}" != "true" ]; then
            echo "✅ praisonai-mcp==${MCP}"
          fi

          if [ "${{ inputs.skip_wrapper }}" != "true" ]; then
            echo "✅ praisonai==${WRAPPER}"
          fi
